<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_widget_container">

    <!-- Content: Bitmap Image -->
    <ImageView
        android:id="@+id/widget_timetable_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:contentDescription="Timetable Grid" />
            
    <!-- Empty State (No Data) -->
    <TextView
        android:id="@+id/widget_empty_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="시간표 데이터가 없습니다"
        android:textColor="@color/text_secondary"
        android:visibility="gone"/>
            
    <!-- Loading State -->
    <ProgressBar
        android:id="@+id/widget_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"/>

    <!-- Minimal Update Time (Optional overlay, right-bottom) 
         If user wants *pure* clean, maybe omit or make very subtle. 
         Let's omit for "Clean" request, or just keep invisible for now. -->
     <TextView
            android:id="@+id/widget_updated_at"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone" />

</FrameLayout>
